apiVersion: v1
kind: Secret
metadata:
  name: gcp-credentials
  namespace: default
stringData:
  credentials.json: |
    {"type": "service_account", "project_id": "YOUR_PROJECT", ...}
# Replace the above with your actual service account JSON
